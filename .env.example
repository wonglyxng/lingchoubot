SERVER_HOST=0.0.0.0
SERVER_PORT=8080

DB_HOST=postgres
DB_PORT=5432
DB_USER=lingchou
DB_PASSWORD=lingchou
DB_NAME=lingchou
DB_SSLMODE=disable

MINIO_ENDPOINT=minio:9000
MINIO_ACCESS_KEY=lingchou
MINIO_SECRET_KEY=lingchou_minio_dev
MINIO_BUCKET=lingchou-artifacts
MINIO_USE_SSL=false

API_KEY=

# LLM 配置（设为 true 并填写 API Key 启用真实 LLM Agent）
LLM_ENABLED=false
LLM_BASE_URL=https://api.openai.com/v1
LLM_API_KEY=
LLM_MODEL=gpt-4o-mini

# Temporal 配置（设为 true 启用 Temporal 工作流编排）
TEMPORAL_ENABLED=false
TEMPORAL_HOST_PORT=localhost:7233
TEMPORAL_NAMESPACE=default
TEMPORAL_TASK_QUEUE=lingchou-orchestrator
