# 《灵筹 MVP 开发进度总结》

## 1. 文档信息

| 字段 | 值 |
|------|------|
| 文档名称 | 灵筹 MVP 开发进度总结 |
| 文档版本 | V0.1 |
| 文档状态 | 随开发进度更新 |
| 最后更新 | 2026-03-29 |
| 关联文档 | `003-MVP 开发路线图`、`004-数据库设计`、`005-API 接口文档` |

---

## 2. 工作包完成状态

| 编号 | 工作包 | 对应周次 | 状态 | 完成日期 |
|------|--------|----------|------|----------|
| WP-01 | 初始化 monorepo、Go API、Docker Compose | 第 1 周 | 已完成 | 2026-03-28 |
| WP-02 | PostgreSQL migrations 与核心 schema | 第 2 周 | 已完成 | 2026-03-28 |
| WP-03 | project / phase / agent / task CRUD 与 Repository | 第 2 周 | 已完成 | 2026-03-29 |
| WP-04 | audit_log 与统一审计写入 | 第 2 周 | 已完成 | 2026-03-29 |
| WP-05 | task_contract 与 task_assignment | 第 3 周 | 已完成 | 2026-03-29 |
| WP-06 | 组织树查询与 handoff_snapshot | 第 3 周 | 已完成 | 2026-03-29 |
| WP-07 | artifact / artifact_version | 第 4 周 | 已完成 | 2026-03-29 |
| WP-08 | review_report 服务 | 第 4 周 | 已完成 | 2026-03-29 |
| WP-09 | approval_request 服务与审批流转 | 第 5 周 | 已完成 | 2026-03-29 |
| WP-10 | 审计时间线聚合查询 | 第 5 周 | 已完成 | 2026-03-29 |
| WP-11 | Agent Runtime 协议与 mock worker | 第 6 周 | 已完成 | 2026-03-29 |
| WP-12 | 最小工作流编排器 | 第 6 周 | 已完成 | 2026-03-29 |
| WP-13 | Tool Gateway 与首批工具 | 第 7 周 | 未开始 | — |
| WP-14 | Web 控制台核心页面 | 第 8 周 | 未开始 | — |
| WP-15 | Demo 脚本与端到端验收 | 第 9 周 | 未开始 | — |

---

## 3. 当前数据库表

| 序号 | 表名 | Migration | 状态 |
|------|------|-----------|------|
| 1 | schema_info | 000001 | 已落地 |
| 2 | project | 000002 | 已落地 |
| 3 | project_phase | 000002 | 已落地 |
| 4 | agent | 000002 | 已落地 |
| 5 | task | 000002 | 已落地 |
| 6 | audit_log | 000002 | 已落地 |
| 7 | task_contract | 000003 | 已落地 |
| 8 | task_assignment | 000003 | 已落地 |
| 9 | handoff_snapshot | 000003 | 已落地 |
| 10 | artifact | 000004 | 已落地 |
| 11 | artifact_version | 000004 | 已落地 |
| 12 | review_report | 000004 | 已落地 |
| 13 | approval_request | 000005 | 已落地 |

**尚未创建的表：** `tool_call`（WP-13）。

---

## 4. 当前已实现 API

### 基础设施
- `GET /healthz` — 健康检查
- `GET /readyz` — 数据库就绪检查
- `GET /api/v1/ping` — 连通性探测

### 核心 CRUD（第 2 周）
- Project: POST / GET / GET{id} / PUT{id} / DELETE{id}
- Phase: POST / GET{projectId} / GET{id} / PUT{id} / DELETE{id}
- Agent: POST / GET / GET{id} / PUT{id} / DELETE{id}
- Task: POST / GET / GET{id} / PUT{id} / PATCH{id}/status / DELETE{id}
- Audit: GET /audit-logs

### 契约与分派（第 3 周）
- TaskContract: POST / GET{id} / PUT{id} / GET{taskId}/contracts / GET{taskId}/contracts/latest
- TaskAssignment: POST / GET / GET{id} / PATCH{id}/status
- HandoffSnapshot: POST / GET / GET{id} / GET{taskId}/latest
- OrgTree: GET /agents/org-tree / GET /agents/{id}/subordinates

### 工件与评审（第 4 周）
- Artifact: POST / GET / GET{id} / POST{id}/versions / GET{id}/versions
- ReviewReport: POST / GET / GET{id}

### 审批与审计时间线（第 5 周）
- ApprovalRequest: POST / GET / GET{id} / POST{id}/decide
- ProjectTimeline: GET /projects/{projectId}/timeline
- TaskTimeline: GET /tasks/{taskId}/timeline

### 工作流编排（第 6 周）
- Orchestrator: POST /api/v1/orchestrator/runs（启动工作流）
- Orchestrator: GET /api/v1/orchestrator/runs（查询运行列表）
- Orchestrator: GET /api/v1/orchestrator/runs/{id}（查询运行详情）

**总计：** 约 50 个接口端点。

---

## 5. 已验证的核心业务链路

| 链路 | 验证状态 |
|------|----------|
| 项目 → 阶段 → 任务 CRUD | 已验证 |
| 任务状态机流转（pending → assigned → in_progress → in_review → revision_required） | 已验证 |
| 任务契约创建与版本查询 | 已验证 |
| 任务分派（主管 → 执行 Agent）与分派历史 | 已验证 |
| Agent 三级组织树查询（PM → Supervisor → Worker） | 已验证 |
| 交接快照创建与查询 | 已验证 |
| 工件创建 + 版本自动递增（v1 → v2） | 已验证 |
| 独立评审报告创建 | 已验证 |
| 评审打回自动联动任务状态（in_review → revision_required） | 已验证 |
| 全链路审计日志写入 | 已验证 |
| 审批请求创建与审批/拒绝 | 待验证 |
| 审批 approved 自动推任务为 completed | 待验证 |
| 审批 rejected 自动推任务为 revision_required | 待验证 |
| 项目级审计时间线聚合查询 | 待验证 |
| 任务级审计时间线聚合查询 | 待验证 |

---

## 6. 当前技术架构

```
lingchoubot/
├── AGENTS.md
├── docker-compose.yml        (postgres + migrate + api + frontend)
├── docs/                     (6 份文档)
├── backend/
│   ├── Dockerfile
│   ├── go.mod
│   ├── cmd/api/main.go       (入口，接入全部 handler)
│   ├── cmd/migrate/main.go   (migration 工具)
│   ├── migrations/            (000001~000004，12 张表)
│   └── internal/
│       ├── config/            (环境变量配置)
│       ├── model/             (7 个模型文件)
│       ├── repository/        (8 个 repo 文件 + db.go)
│       ├── service/           (8 个 service 文件)
│       ├── handler/           (11 个 handler 文件，含 orchestrator)
│       ├── runtime/           (Agent Runtime 协议 + 4 个 Mock Agent)
│       ├── orchestrator/      (工作流引擎 + 运行状态追踪)
│       └── middleware/        (cors / logging / recovery / response)
└── frontend/                  (Next.js 壳，尚未开始业务页面)
```

---

## 7. 下一步计划

第 6 周（WP-11/12）已完成。按路线图，下一步进入 **第 7 周：Tool Gateway**（WP-13）：

1. **WP-13：Tool Gateway 抽象 + tool_call 记录表 + 首批工具接入**

验收标准：
- Agent 可通过 Tool Gateway 调用至少 2 个工具
- 每次调用都有 tool_call 记录
- 未授权调用会被拒绝
- 失败结果能反馈回任务状态

---

## 8. 已知假设与待决事项

| 项目 | 说明 |
|------|------|
| MinIO 文件上传 | 工件模型已支持 URI 元数据入库，但实际文件上传（MinIO 对接）尚未实现 |
| 权限校验 | MVP 阶段暂无鉴权中间件，所有 API 开放访问 |
| 前端 | Next.js 项目已初始化，但尚未开始业务页面开发（第 8 周计划） |
| 测试 | 关键链路已通过 curl 手动验证，尚无自动化单元/集成测试 |
| Agent Runtime | Mock Agent 和工作流编排器已实现（WP-11/12 完成） |

---

## 9. 变更记录

| 日期 | 版本 | 变更内容 |
|------|------|----------|
| 2026-03-29 | V0.1 | 初始版本：记录 WP-01 ~ WP-08 完成状态，12 张表，约 40 个 API 端点 |
| 2026-03-29 | V0.2 | WP-09/10 完成：approval_request + 审计时间线聚合查询，13 张表，约 47 个端点 |
| 2026-03-29 | V0.3 | WP-11/12 完成：Agent Runtime 协议 + 4 个 Mock Agent + 工作流编排器，约 50 个端点 |
